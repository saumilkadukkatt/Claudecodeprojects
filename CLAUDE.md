# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Overview

This is a general-purpose projects repository (`claudecodeprojects`). Currently it contains:

- `SAPSecurityTaxanomy/` — a directory for SAP security taxonomy content (currently empty)

There is no build system, test runner, or package manager configured at the root level. Each subdirectory/project may have its own tooling; check within that directory for a README, `package.json`, `requirements.txt`, `Makefile`, or similar before running any commands.

## Development Branch

All feature work should be developed on the branch `claude/init-JmixF` and pushed with:

```bash
git push -u origin claude/init-JmixF
```
